# Aniki plans

Feature slices use `.cursor/plans/<slug>/`. Templates: [_templates/](_templates/).

Do not store GP FSA/epic-queue factories here. Handoff cards are the contract between zones (see `AGENTS.md`).
